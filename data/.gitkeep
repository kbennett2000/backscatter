# This file keeps the data/ directory in the repo so a fresh clone already has it
# owned by you. Docker would otherwise create the bind-mount source as root, and the
# non-root container (user: ${PUID:-1000}) couldn't write the database. Your radar
# archive lands in this folder at runtime and is git-ignored.
